<Page x:Class="TRPO.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">

    <Grid>
        <Label VerticalAlignment="top" HorizontalAlignment="Center" Margin="0,100,0,0" FontSize="18">Настройки</Label>
        <ComboBox x:Name="ThemeComboBox" VerticalAlignment="Center" HorizontalAlignment="Center" Width="150" Padding="10">
            <ComboBoxItem Name="LightTheme">
                <StackPanel>
                    <TextBlock FontSize="16">Светлая</TextBlock>
                    <TextBlock>По-умолчанию</TextBlock>
                </StackPanel>
            </ComboBoxItem>
            <ComboBoxItem Name="DarkTheme">
                <StackPanel>
                    <TextBlock FontSize="16">Темная</TextBlock>
                    <TextBlock>Ночь</TextBlock>
                </StackPanel>
            </ComboBoxItem>
        </ComboBox>
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Center" Padding="10" Click="Exit_Click">Выход</Button>
    </Grid>
</Page>
